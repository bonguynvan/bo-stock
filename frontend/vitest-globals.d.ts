// Additive global type references for the test suite so `tsc --noEmit` stays
// clean without restricting the app build's `types` resolution.
/// <reference types="vitest/globals" />
/// <reference types="@testing-library/jest-dom/vitest" />
